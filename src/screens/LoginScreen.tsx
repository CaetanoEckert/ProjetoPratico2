import React,{useState}from'react';import{Alert}from'react-native';import{Button,Field,Screen,AppText}from'../components/UI';import{useAuth}from'../hooks/useAuth';import{validateEmail,validatePassword}from'../utils/validation';
export function LoginScreen({navigation}:any){const{signIn}=useAuth();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[errors,setErrors]=useState<any>({});async function submit(){const e={email:validateEmail(email),password:validatePassword(password)};setErrors(e);if(e.email||e.password)return;try{await signIn(email,password)}catch(err:any){Alert.alert('Erro no login',err.message)}}return <Screen><AppText title>TaskList App</AppText><AppText muted>Entre para gerenciar suas listas.</AppText><Field label="E-mail" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" error={errors.email}/><Field label="Senha" value={password} onChangeText={setPassword} secureTextEntry error={errors.password}/><Button title="Entrar" onPress={submit}/><Button title="Criar conta" variant="ghost" onPress={()=>navigation.navigate('Register')}/><Button title="Recuperar senha" variant="ghost" onPress={()=>navigation.navigate('ForgotPassword')}/></Screen>}

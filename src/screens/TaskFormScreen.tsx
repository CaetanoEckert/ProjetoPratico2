import React,{useState}from'react';import{Alert}from'react-native';import{Button,Field,Screen,AppText}from'../components/UI';import{saveTask}from'../services/taskService';import{validateName}from'../utils/validation';
export function TaskFormScreen({navigation,route}:any){const{listId,task}=route.params;const[title,setTitle]=useState(task?.title??'');const[description,setDescription]=useState(task?.description??'');const[error,setError]=useState('');async function submit(){const e=validateName(title,'Título da atividade');setError(e);if(e)return;try{await saveTask(listId,title.trim(),description.trim(),task);navigation.goBack()}catch(err:any){Alert.alert('Erro',err.message)}}return <Screen><AppText title>{task?'Editar atividade':'Nova atividade'}</AppText><Field label="Título" value={title} onChangeText={setTitle} error={error}/><Field label="Descrição (opcional)" value={description} onChangeText={setDescription} multiline/><Button title="Salvar" onPress={submit}/></Screen>}

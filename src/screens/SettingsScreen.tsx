import React from'react';import{Button,Card,Screen,AppText}from'../components/UI';import{useAuth}from'../hooks/useAuth';import{useTheme}from'../hooks/useTheme';
export function SettingsScreen({navigation}:any){const{user,signOut}=useAuth();const{mode,toggleTheme}=useTheme();return <Screen><Button title="Voltar" variant="ghost" onPress={()=>navigation.goBack()}/><AppText title>Configurações</AppText><Card><AppText>Usuário: {user?.email}</AppText><AppText>Tema atual: {mode==='light'?'claro':'escuro'}</AppText><Button title="Alternar tema claro/escuro" onPress={toggleTheme}/><Button title="Sair" variant="danger" onPress={signOut}/></Card></Screen>}
